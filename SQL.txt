
--INNER JOIN
SELECT E.NOMBRE, D.NOMBRE, L.NOMBRE
FROM EMPLEADOS E, DEPARTAMENTO D, LOCALIDAD L
WHERE E.DEPARTAMENT_ID=D.DEPARTAMENT_ID(+) 
	AND
	D.LOCALIDAD_ID=L.LOCALIDAD_ID(+);




--ERROR
SELECT E.NOMBRE, D.NOMBRE, L.NOMBRE
FROM EMPLEADOS E, DEPARTAMENTO D, LOCALIDAD L
WHERE E.DEPARTAMENT_ID=D.DEPARTAMENT_ID(+) 
	AND
	D.LOCALIDAD_ID(+)=L.LOCALIDAD_ID;




--DATE
TO_CHAR(FECHA_ALTA, 'DD/MM/YYYY, HH24:mi:ss')


--LIKE

WHERE NOMBRE LIKE 	'A%'  -- comienza con A
			'_A%' -- un caracter seguido de A y lo que sea
			'A_%' -- A un caracter y lo que sea

--EFICIENCIA EN FROM Y WHERE LOS DE LA IZQUIERDA SE HACE PRIMERO
SELECT *
FROM EMPLEADOS
WHERE ID_DEPARTAMENTO = 2
	AND WHERE NOMBRE LIKE 'A%'


--JOIN ES MAS EFICIENTE QUE EL IN
